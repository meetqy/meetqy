!!! MEETQT-NO-SERVER-TEMPLATE !!!

## 通用规则

- 回复使用中文；代码注释用中文；界面文案默认英语。
- 每次回复第一行固定输出 `!!! MEETQT-NO-SERVER-TEMPLATE !!!`。
- 不要过度设计，保持代码简单、可读、实用。
- 优先复用已有模块与能力，控制圈复杂度。
- 改动最小化，避免修改无关文件与逻辑。
- 除非用户明确要求，否则不要执行 `pnpm check:write`。
- 每次开始编码前，先阅读项目根目录下的 `DESIGN.md`，了解设计规范与技术选型。
- 使用 shadcn ui 库，除非用户明确要求，否则不要自定义组件，直接使用 shadcn 组件的默认样式。
- 安装依赖时，使用 `pnpm`。
